/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SDKHooks } from "../hooks";
import { SDKOptions, serverURLFromOptions } from "../lib/config";
import { HTTPClient } from "../lib/http";
import { ClientSDK } from "../lib/sdks";
import { Disputes } from "./disputes";
import { Locations } from "./locations";
import { Metrics } from "./metrics";
import { Orders } from "./orders";
import { PaymentMethods } from "./paymentmethods";
import { ProductCategories } from "./productcategories";
import { Products } from "./products";
import { Reports } from "./reports";
import { SDKCustomers } from "./sdkcustomers";
import { SDKPayments } from "./sdkpayments";
import { SDKSalesTransactions } from "./sdksalestransactions";

export class Sales extends ClientSDK {
    private readonly options$: SDKOptions & { hooks?: SDKHooks };

    constructor(options: SDKOptions = {}) {
        const opt = options as unknown;
        let hooks: SDKHooks;
        if (
            typeof opt === "object" &&
            opt != null &&
            "hooks" in opt &&
            opt.hooks instanceof SDKHooks
        ) {
            hooks = opt.hooks;
        } else {
            hooks = new SDKHooks();
        }

        super({
            client: options.httpClient || new HTTPClient(),
            baseURL: serverURLFromOptions(options),
            hooks,
        });

        this.options$ = { ...options, hooks };
        void this.options$;
    }

    private _metrics?: Metrics;
    get metrics() {
        return (this._metrics ??= new Metrics(this.options$));
    }

    private _reports?: Reports;
    get reports() {
        return (this._reports ??= new Reports(this.options$));
    }

    private _customers?: SDKCustomers;
    get customers() {
        return (this._customers ??= new SDKCustomers(this.options$));
    }

    private _disputes?: Disputes;
    get disputes() {
        return (this._disputes ??= new Disputes(this.options$));
    }

    private _locations?: Locations;
    get locations() {
        return (this._locations ??= new Locations(this.options$));
    }

    private _orders?: Orders;
    get orders() {
        return (this._orders ??= new Orders(this.options$));
    }

    private _paymentMethods?: PaymentMethods;
    get paymentMethods() {
        return (this._paymentMethods ??= new PaymentMethods(this.options$));
    }

    private _payments?: SDKPayments;
    get payments() {
        return (this._payments ??= new SDKPayments(this.options$));
    }

    private _productCategories?: ProductCategories;
    get productCategories() {
        return (this._productCategories ??= new ProductCategories(this.options$));
    }

    private _products?: Products;
    get products() {
        return (this._products ??= new Products(this.options$));
    }

    private _transactions?: SDKSalesTransactions;
    get transactions() {
        return (this._transactions ??= new SDKSalesTransactions(this.options$));
    }
}
