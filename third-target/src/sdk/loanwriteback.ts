/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SDKHooks } from "../hooks";
import { SDKOptions, serverURLFromOptions } from "../lib/config";
import { HTTPClient } from "../lib/http";
import { ClientSDK } from "../lib/sdks";
import { BankAccounts } from "./bankaccounts";
import { BankTransactions } from "./banktransactions";
import { CreateOperations } from "./createoperations";
import { SDKDirectCosts } from "./sdkdirectcosts";
import { SDKLoanWritebackAccounts } from "./sdkloanwritebackaccounts";
import { SDKLoanWritebackPayments } from "./sdkloanwritebackpayments";
import { SDKSuppliers } from "./sdksuppliers";
import { SDKTransfers } from "./sdktransfers";

export class LoanWriteback extends ClientSDK {
    private readonly options$: SDKOptions & { hooks?: SDKHooks };

    constructor(options: SDKOptions = {}) {
        const opt = options as unknown;
        let hooks: SDKHooks;
        if (
            typeof opt === "object" &&
            opt != null &&
            "hooks" in opt &&
            opt.hooks instanceof SDKHooks
        ) {
            hooks = opt.hooks;
        } else {
            hooks = new SDKHooks();
        }

        super({
            client: options.httpClient || new HTTPClient(),
            baseURL: serverURLFromOptions(options),
            hooks,
        });

        this.options$ = { ...options, hooks };
        void this.options$;
    }

    private _bankAccounts?: BankAccounts;
    get bankAccounts() {
        return (this._bankAccounts ??= new BankAccounts(this.options$));
    }

    private _accounts?: SDKLoanWritebackAccounts;
    get accounts() {
        return (this._accounts ??= new SDKLoanWritebackAccounts(this.options$));
    }

    private _suppliers?: SDKSuppliers;
    get suppliers() {
        return (this._suppliers ??= new SDKSuppliers(this.options$));
    }

    private _transfers?: SDKTransfers;
    get transfers() {
        return (this._transfers ??= new SDKTransfers(this.options$));
    }

    private _bankTransactions?: BankTransactions;
    get bankTransactions() {
        return (this._bankTransactions ??= new BankTransactions(this.options$));
    }

    private _directCosts?: SDKDirectCosts;
    get directCosts() {
        return (this._directCosts ??= new SDKDirectCosts(this.options$));
    }

    private _payments?: SDKLoanWritebackPayments;
    get payments() {
        return (this._payments ??= new SDKLoanWritebackPayments(this.options$));
    }

    private _createOperations?: CreateOperations;
    get createOperations() {
        return (this._createOperations ??= new CreateOperations(this.options$));
    }
}
