/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as z from "zod";

/**
 * Reference that links the line item to the correct product details.
 */
export type ProductRef = {
    /**
     * The unique identifier of the product being referenced.
     */
    id: string;
    /**
     * Name of the product being referenced.
     */
    name?: string | undefined;
};

/** @internal */
export namespace ProductRef$ {
    export type Inbound = {
        id: string;
        name?: string | undefined;
    };

    export const inboundSchema: z.ZodType<ProductRef, z.ZodTypeDef, Inbound> = z
        .object({
            id: z.string(),
            name: z.string().optional(),
        })
        .transform((v) => {
            return {
                id: v.id,
                ...(v.name === undefined ? null : { name: v.name }),
            };
        });

    export type Outbound = {
        id: string;
        name?: string | undefined;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, ProductRef> = z
        .object({
            id: z.string(),
            name: z.string().optional(),
        })
        .transform((v) => {
            return {
                id: v.id,
                ...(v.name === undefined ? null : { name: v.name }),
            };
        });
}
